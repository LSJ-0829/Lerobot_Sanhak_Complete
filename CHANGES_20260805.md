# 2026-08-05 변경 사항과 원복 방법

시연 본체(`lerobot@115.145.179.95`)와 Jetson(`comnet02@192.168.55.1`)에 가한 변경 전부.
**git 밖의 변경**(남의 레포·시스템 파일)은 반드시 아래 방법으로 되돌릴 것.

---

## A. 우리 레포 (LSJ / Lerobot_Sanhak_Complete) — git 으로 원복

| 파일 | 내용 |
|---|---|
| `scripts/skills/laundry_task3.py` | 순응 물기, 그리퍼 벌림, y 허용오차 4, 회전속도 deg/s 수정 |
| `scripts/skills/laundry_task4.py` | Jetson 링크·host 자동 준비, 레지스터 자동 설정, preflight(모델→홈자세→카메라), 폴딩 최대 10회 재시작 |
| `examples/lekiwi/lekiwi_pose.py` | `move_to_pose` 도달 대기 |
| `red_approach.json` | CENTER_X 346 / TARGET_Y 17 |
| `examples/lekiwi/resolve_cameras.py` | (신규) 카메라 4대를 모델+화면내용으로 배정 |
| `examples/lekiwi/record_idle_posture.py` | (신규) 현재 양팔 자세를 `idle_posture.json` 으로 저장 |
| `examples/lekiwi/patch_csi_camera_paths.py` | (신규) csi-agent 카메라 경로 패치 도구 |
| `examples/lekiwi/patch_csi_motor_retry.py` | (신규) csi-agent 모터 재연결 패치 도구 |
| `examples/lekiwi/usb_reset.py` | (신규) 카메라 USB 전원 리셋(sudo 불필요) |

```bash
cd ~/lerobot2/csi-agent/LSJ
git log --oneline          # 되돌릴 커밋 찾기
git revert <커밋>          # 개별 되돌리기
```

핵심 상수(환경변수로 즉시 덮어쓰기 가능, 파일 수정 불필요):

```bash
V_ROT=10.55            # 회전 deg/s. rad/s 아님 — host 가 내부에서 rad 로 바꾼다
ROTATE_SEC=16.9        # 180도
APPROACH_Y_TOL=4       # 넓히려면 6
COMPLIANT_GRIP=0       # 순응 물기 끄기
GRIP_STALL_SEC=0.5     # 물림 감지 지속시간
```

---

## B. Jetson `lekiwi.py` — **vendored 파일 수정** ⚠️

`~/lerobot/src/lerobot/robots/lekiwi/lekiwi.py` 의 `configure()` 에 두 가지를 심었다.
**host 가 connect 할 때마다 이 레지스터들을 되돌려 쓰기 때문에**, 기동 전 설정으로는
해결이 안 되어 connect 경로 안에서 고쳐야 했다.

1. gripper(6번) `P_Coefficient` 16 → 32 — 그립이 약해 손잡이를 놓치던 원인
2. 팔 1~6번 `Goal_Velocity` 800 / `Acceleration` 40 — 모션이 뚝뚝 끊기던 원인
   (기본 0/254 = 속도 무제한 + 즉시 가속 → 웨이포인트마다 튀어가 멈춤)

**원복:**
```bash
ssh comnet02@192.168.55.1
cp ~/lerobot/src/lerobot/robots/lekiwi/lekiwi.py.bak.orig \
   ~/lerobot/src/lerobot/robots/lekiwi/lekiwi.py
# host 재기동해야 반영됨
pkill -f lekiwi_host_keepalive.sh; pkill -f lerobot.robots.lekiwi.lekiwi_host
```
**상태 확인:** `python3 ~/lekiwi_patch.py --check`

---

## C. Jetson 새 파일 (원본 수정 아님 — 지우면 끝)

| 파일 | 용도 |
|---|---|
| `~/lekiwi_host_keepalive.sh` | host 자동 재기동 + `flock` 단일 인스턴스 보장 |
| `~/.lekiwi_host.lock` | 위 잠금 파일 |
| `~/lekiwi_patch.py` | B 패치 적용/확인 도구 |
| `~/lekiwi_grip_regs.py` | 6번 순응 레지스터 확인/설정 |
| `~/lekiwi_arm_speed.py` | 팔 속도·가속 확인/설정 |

원복: 파일 삭제 + `pkill -f lekiwi_host_keepalive.sh`. 그 뒤에는 예전처럼 손으로 host 를 띄워야 한다.

---

## D. csi-agent `rollout.py` — **남의 레포 수정** ⚠️

`robot.connect()` 2곳을 `_connect_with_retry()` 로 감쌌다(최대 5회, 대기 2.0→3.0→4.5→6.75초).
SO101 팔이 가끔 첫 연결에서 인식되지 않아 파이프라인이 통째로 죽던 것을 막는다.

**원복:**
```bash
cd ~/lerobot2/csi-agent/LSJ
python examples/lekiwi/patch_csi_motor_retry.py --revert
```
백업: `~/lerobot2/csi-agent/lhwdev/clothing/rollout.py.bak.motorretry`
**상태 확인:** 같은 스크립트에 `--check`

---

## E. csi-agent 체크포인트 심볼릭 링크 수정 ⚠️

`lhwdev/train/rollout_hil_towel_fold01_step0/checkpoints/last` 가 **깨져 있었다** —
이 디렉터리에 없는 `074740`(옆 디렉터리 `towel_fold01_step0` 의 번호)을 가리키고 있었다.
이 디렉터리의 최고 번호이자 추론 검증을 통과한 `027000` 으로 바꿨다.

**원복:**
```bash
cd ~/lerobot2/csi-agent/lhwdev/train/rollout_hil_towel_fold01_step0/checkpoints
ln -sfn 074740 last     # 원래(깨진) 값으로
```

파이프라인은 `--no_step0` 으로 돌아 이 정책을 쓰지 않으므로 현재 시연에는 영향이 없다.
`--no_step0` 을 뗄 때를 대비한 수리다.

---

## F. 시스템 설정 (사용자가 직접 적용)

- `/etc/sudoers.d/lekiwi-usbnet` — `lekiwi-usbnet.service` 재시작만 무암호 허용
  → 원복: `sudo rm /etc/sudoers.d/lekiwi-usbnet`
- `/etc/udev/rules.d/99-lekiwi-usbnet.rules` — **0바이트(비어 있음)**. 채우려면:
  ```bash
  echo 'ACTION=="add", SUBSYSTEM=="net", KERNEL=="enx*", RUN+="/bin/systemctl restart --no-block lekiwi-usbnet.service"' | sudo tee /etc/udev/rules.d/99-lekiwi-usbnet.rules && sudo udevadm control --reload
  ```
- 분류기 `model.safetensors` 권한 600 → 읽기 가능으로 변경됨(그대로 두어야 함)

---

## G. 카메라 배정 — csi-agent `setup_devices.py` 수정 ⚠️ + 새 파일

카메라 4대가 **2쌍씩 벤더·모델·시리얼이 완전히 같아서** udev 가 구분할 단서가 물리 포트뿐이다.
그래서 자리를 옮기면 `/dev/lerobot/camera_N` 이 통째로 뒤바뀐다 —
실제로 폴딩 `top` 이 세탁기 상공캠을 가리킨 채로 돌아갔다(그 상태에서 분류기가
빈 판을 `step1` 로 확신했다).

- 새 파일 `examples/lekiwi/resolve_cameras.py` 가 매 실행마다 **모델명으로 2쌍을 가르고,
  화면 내용으로 쌍 안을 가른다**(어두운 화면 비율 → 상공/폴딩 top, 밝은 쪽 무게중심 → 좌/우).
  결과는 `~/.lerobot/cams/{top,left_cam,right_cam,overhead}` 링크로 쓴다.
- `setup_devices.py` 는 그 링크가 있으면 쓰고 없으면 기존 `/dev/lerobot/camera_N` 을 쓴다.

**원복:**
```bash
cd ~/lerobot2/csi-agent/LSJ
python examples/lekiwi/patch_csi_camera_paths.py --revert
rm -rf ~/.lerobot/cams ~/.lerobot/cams.json
```
백업: `~/lerobot2/csi-agent/lhwdev/clothing/scripts/setup_devices.py.bak.campaths`
**끄기만:** `python laundry_task4.py --no-cam-resolve`

좌/우가 반대로 나오면(폴딩에서 엉뚱한 팔이 움직이면):
```bash
python examples/lekiwi/resolve_cameras.py --set-left-x-side left   # 기본값은 right
```

---

## H. 홈 자세 — `idle_posture.json` **신규 생성**

csi-agent 의 `RobotHoming.load_idle_posture()` 는 `clothing/idle_posture.json` 을 찾는데
**그 파일이 아예 없어서** 항상 `None` 이었다 → 홈 복귀가 한 번도 돌지 않았다
("팔이 제자리로 세팅되지 않는다"의 원인). 사용자가 잡아준 자세를 그대로 저장했다.

**원복:** `rm ~/lerobot2/csi-agent/lhwdev/clothing/idle_posture.json`
(이전 값이 있으면 `idle_posture.json.bak`)
**끄기만:** `python laundry_task4.py --no-home`
**다시 잡기:** 팔을 원하는 자세로 두고 `python examples/lekiwi/record_idle_posture.py`

---

## I. 폴딩 프로세스 재시작 (laundry_task4 안 — git 원복 대상)

모터·카메라가 **가끔 이유 없이** 안 잡힌다(하드웨어 고장이 아니라 버스/장치가 아직
정리되지 않은 상태에서 여는 것). 실패 지점이 `rollout.py` 일 수도 `setup_devices.py` 일 수도
있어서, 호출 하나를 감싸는 대신 **프로세스를 통째로 최대 10회 다시 띄운다**.

```bash
python laundry_task4.py --csi-retries 1        # 재시도 끄기
python laundry_task4.py --csi-retry-delay 10   # 간격 늘리기
```

---

## J. 모델 파일 preflight (laundry_task4 안 — git 원복 대상)

모델은 `lhwdev` 계정이 만들고 우리는 `lerobot` 으로 읽기만 한다. `cp -p` / `rsync -a` 는
원본 권한을 그대로 들고 와 `train/` 에 걸린 default ACL 을 덮어쓴다 —
분류기가 `-rw-------` 로 들어와 롤아웃이 죽은 적이 있다(2026-08-05).
폴딩은 파이프라인의 끝이라 여기서 죽으면 **수건을 이미 전달받은 뒤**다.

그래서 Enter 를 받기 전에 실제로 열릴 5개(정책 4 + 분류기)를 확인한다.
`rollout_auto.py` 와 같은 방식으로 경로를 계산하고(`--no_step0` 이면 step0 은 제외),
링크가 끊겼는지 · 읽을 수 있는지 · safetensors 가 **끝까지 있는지**(헤더 오프셋 vs 파일 크기)를 본다.
읽기 권한만 없고 우리가 주인이면 `a+r` 로 스스로 고친다.

주인이 달라 못 고치면(현재 `lerobot` 은 무암호 sudo 가 `lekiwi-usbnet` 뿐이다) 이렇게 안내한다:
```bash
sudo chmod -R a+rX /home/lerobot/lerobot2/csi-agent/lhwdev/train
```
재발을 막으려면 lhwdev 쪽에서 복사할 때 `-p`/`-a` 대신 권한을 안 들고 오게 하거나,
복사 뒤 위 `chmod` 를 한 번 돌리면 된다(`train/` 의 default ACL 은 **새로 만든** 파일에만 적용된다).

**끄는 옵션은 없다** — 확인이 즉시 끝나고 로봇을 건드리지 않아서 끌 이유가 없다.
꼭 건너뛰려면 `check_models()` 호출을 preflight 에서 빼면 된다.

---

## K. 카메라 USB 전원 리셋 (laundry_task4 안 — git 원복 대상)

**증상:** USB 에는 4대가 다 열거되는데 그중 몇 대는 `bConfigurationValue` 가 비어 있고
uvcvideo 가 안 붙어 `/dev/video*` 노드가 아예 생기지 않는다. 뽑았다 꽂으면 낫는다.
2026-08-05 18:24 시점에 실제로 2대(1-3, 1-7)가 이 상태였다.

`examples/lekiwi/usb_reset.py` 가 `authorized` 0→1 토글로 뽑았다 꽂는 것과 같은 일을 한다
(안 되면 `USBDEVFS_RESET` ioctl 보조). **sudo 불필요** — `99-lerobot.rules:47` 이 카메라
(VID:PID `0c45:6366`)의 `authorized` 를 0666 으로 열어 둔다.

거는 곳 세 군데:
1. preflight 폴딩캠 배정 — 배정 실패하면 리셋 후 재시도, **최대 5회**
2. 폴딩 재시도 루프 — 실패 로그가 카메라 쪽이면(`could not be opened`, `device busy`,
   `VIDIOC_` …) 리셋 후 다시 배정
3. `ensure_cams_valid` — 띄우기 직전 USB 상태까지 확인

```bash
python examples/lekiwi/usb_reset.py            # 상태만 본다
python examples/lekiwi/usb_reset.py --recover  # 필요하면 되살린다
python laundry_task4.py --cam-resets 0         # 리셋 끄기
CAM_POWER_OFF_SEC=3.0 python laundry_task4.py  # 전원 내린 채 두는 시간(기본 2.0초)
```

리셋하면 `/dev/videoN` 번호가 통째로 바뀌므로 **반드시 뒤이어 재배정**한다(G 항목이 그 일).
실측: 2대 죽은 상태 → 리셋 1회 → 배정 완료까지 12.7초.

**보이지 않는 카메라는 못 살린다.** USB 열거 자체가 안 된 장치는 리셋할 대상이 없어서,
한 번 시도하고 "케이블/허브를 확인하세요"로 멈춘다(무한 재시도하지 않는다).

> 참고: csi-agent 에도 `tools/usb_reset.py` 가 있고 `setup_devices.py`/`rollout.py` 가
> 연결 실패 뒤 `reset_usb_cameras(power_cycle=True, reset_all=True)` 로 부른다.
> 그건 **실행 중 실패한 뒤**에만 돌고 포트가 `["1-3","1-5","1-7"]` 로 박혀 있다
> (4번째 카메라 1-6.3 은 목록에 없고, 자리가 바뀌면 엉뚱한 포트를 리셋한다).
> 우리 쪽은 **시작 전에**, 포트가 아니라 VID:PID 로 찾아서 건다. 둘은 충돌하지 않는다.

---

## L. VLA 대신 규칙기반 집기 (`--rulebased`)

VLA 는 GPU·체크포인트·관측 파이프라인이 전부 맞아야 돈다. 하나라도 어긋나면 [3]에서
통째로 멈추는데, 그 앞(접근·문열기)이 이미 끝난 뒤라 손해가 크다.
laundry_task2 가 쓰던 방식 — **녹화 모션 재생 → probe 판정 → 실패하면 재시도** — 을 대안으로 뒀다.

```bash
python laundry_task4.py --rulebased                          # 기본 모션 laundry_grab, 3회
python laundry_task4.py --rulebased --grab-motion grab_ver.7 --grab-attempts 5
RULEBASED=1 python laundry_task3.py                          # task3 단독으로도
```

- `--rulebased` 면 **SmolVLA 를 아예 로드하지 않는다.** '작동하지 않을 때의 대비'인데
  로딩에서 죽으면 의미가 없다. probe(상공·손목)는 판정에 계속 쓴다.
- 판정 게이트는 VLA 판과 **완전히 같은 것**이라 '잡았다'의 기준이 달라지지 않는다.
  상공(수건이 들렸나) → 손목(실제로 물렸나) 순서도 그대로.
- 모션 파일이 없으면 preflight 단계에서 exit 9 로 멈춘다([3]에서야 알면 늦다).
- 집기 실패는 **exit 10**. 6(throw 모션 실패)과 겹치지 않게 새 번호를 썼다.

**task2 를 그대로 돌리지 않고 '방식'만 옮긴 이유:** task2 는 Jetson 에서 시리얼 버스를
직접 잡는다(`common.py`: `REPO=/home/comnet02/lerobot`, `--port /dev/ttyACM0`).
지금 구조는 `lekiwi_host` 가 그 버스를 쥐고 있어서 그대로는 못 돌린다 —
host 를 내리면 task4 의 나머지가 전부 죽는다. 그래서 판정·재시도 로직만 ZMQ 쪽으로 옮겼다.

### ⚠️ 모션 시작 자세 — 실측으로 잡은 문제

`replay_motion` 은 프레임을 `interval`(0.3초)마다 그대로 던진다. 모션의 첫 프레임이
지금 자세와 멀면 팔이 그 거리를 0.3초 만에 가려고 튄다. 실측한 차이:

| 모션 | `laundry_grabready` 대비 최대 차이 |
|---|---|
| `laundry_grab` (task2 기본값) | **105.9°** (shoulder_lift), wrist_flex 105.2° |
| `grab` | 19.6 |
| `grab_ver.7` | 19.6 |

그래서 재생 전에 **모션의 첫 프레임으로 먼저 3초에 걸쳐 부드럽게 이동**한다
(`motion_first_action()`). 이 처리 덕에 어떤 모션을 골라도 안전하고,
task2 의 원래 기본값 `laundry_grab` 을 그대로 기본값으로 둘 수 있다.

---

## 전부 원복하는 순서

```bash
cd ~/lerobot2/csi-agent/LSJ
python examples/lekiwi/patch_csi_motor_retry.py --revert                 # D
python examples/lekiwi/patch_csi_camera_paths.py --revert                # G
rm -rf ~/.lerobot/cams ~/.lerobot/cams.json                              # G
rm ~/lerobot2/csi-agent/lhwdev/clothing/idle_posture.json                # H
ssh comnet02@192.168.55.1 'cp ~/lerobot/src/lerobot/robots/lekiwi/lekiwi.py.bak.orig \
    ~/lerobot/src/lerobot/robots/lekiwi/lekiwi.py'                       # B
cd ~/lerobot2/csi-agent/lhwdev/train/rollout_hil_towel_fold01_step0/checkpoints && ln -sfn 074740 last  # E
cd ~/lerobot2/csi-agent/LSJ && git revert <커밋들>                        # A, I, J, K
```
